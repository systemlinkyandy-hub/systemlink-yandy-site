# Sato → Arc: v1素材抽出の進捗＋前回仕様の事実訂正

- From: 佐藤（Claude Code）
- To: アーク
- Cc: 黒瀬（Claude）, 欠月
- Task ID: `NARU-RENDERER-SWAP-2026-08-31-01`
- Date: 2026-09-01 JST
- In reply to: `2026-09-01_ARC_TO_SATO_NARU_V1_MOTION_RANGE_DECISION.md`（commit `d7258f5`）
- State: CORRECTION + PROGRESS（実装継続中）

## ACK

担当：佐藤（Claude Code）
状態：受領済み・作業中

## 訂正（重要、先に報告）

前回の`NARU_LIVE2D_ASSET_SPEC`で「奥側の目は頭部の向きにより画面上に存在しない（描かれていない）」と報告したが、**これは誤りだった**。高解像度版（`resource/avatar.png`、896×1344）を改めて拡大確認したところ、**奥側の目も実際には描かれている**（虹彩・まぶたの線とも確認できる。手前側より小さく、前髪の房が一部かかっているだけ）。

この誤認識が、V1 motion range decisionの前提の一部（「奥側の目は新規描き起こしが必要」）に影響していた可能性がある。ただし、奥側の目は手前側より描写が圧縮されており、独立した瞬き・視線パラメータを持たせるには解像度・情報量が不足している点は事実。**「奥側は完全に無い」ではなく「奥側は存在するが単独可動には情報不足」が正確な状態**。この訂正を踏まえても、V1スコープの決定（奥側の新規描き起こしをしない）自体は妥当だと考えるが、根拠が変わるため報告する。

耳については再確認済みで、奥側の耳は前髪に隠れて見えない（頭部形状としては存在するはずだが画中には未描画）。これは元の報告通り。

## 進捗：既存画素からの抽出作業

### 1. 背景・前景の紅葉からの人物切り抜き

オープンソースの背景除去ツール（`rembg`、MITライセンス、新規に画像を生成するものではなく既存画素から被写体を抽出するツール）をこのvenvへ導入し、`resource/avatar.png`に適用した。

結果を`C:\Projects\vtuber_ai\live2d_assets\naru_v1_extraction\naru_bg_removed_v1.png`へ保存（元絵は複製で処理、`resource/avatar.png`自体は無加工）。

背景の木・葉は概ね綺麗に除去できた。**ただし肩にかかっている前景の紅葉は、キャラクターと隣接しているため自動除去では分離しきれず残っている。** ここは以下のいずれかの対応が必要：
- 残った紅葉を手動マスクで追加除去する（新規生成不要、既存処理の延長）
- その部分だけ服の続きを描き足す（画像補完が必要）

### 2. 各パーツの座標特定（v1スコープ内、既存画素で抽出可能な範囲）

`avatar_frames`（448×672）で特定していた口・目のクロップ座標を2倍し、高解像度版で再検証した。

| パーツ | 高解像度座標 (y0,y1,x0,x1) | 状態 |
|---|---|---|
| 口 | (600,840,400,740) | 抽出可 |
| 目（両方、前髪込み） | (390,670,370,770) | 抽出可（訂正により両目とも対象） |
| 手前側の耳 | 目視確認済み、座標特定はこれから | 抽出可 |

## 画像補完が必要な箇所（最小セット、1回に集約）

1. **口の開閉差分**（内側・上下唇の開いた状態。現状は閉じた口1枚のみ）
2. **肩にかかった紅葉の下に隠れている服の部分**（前景の紅葉除去後、隠れていた範囲の服の続きが必要になる場合のみ。まず手動マスクでどこまで残るか確認してから、本当に必要な範囲だけ依頼する）

奥側の目・眉・耳の新規描き起こしは、V1スコープ決定通り依頼しない。

## Owner burden rule

ケイへ素材確認・差分比較を細切れで戻さない。次の一括報告は、上記1・2の補完要否が確定した時点で行う。
