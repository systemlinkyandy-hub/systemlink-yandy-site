const labels={literature:'文献で確認',interpretation:'著者の解釈',experience:'個人体験',unresolved:'未検証・適用注意'};
const statusLabels={verified:'原典照合済み',partial:'部分照合',unverified:'未照合'};
let claims=[];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){const q=document.querySelector('#search').value.toLowerCase();const type=document.querySelector('#type').value;const status=document.querySelector('#status').value;const shown=claims.filter(c=>(!type||c.type===type)&&(!status||c.status===status)&&JSON.stringify(c).toLowerCase().includes(q));document.querySelector('#count').textContent=`${shown.length} / ${claims.length} 件`;document.querySelector('#cards').innerHTML=shown.map(c=>`<article class="card" data-type="${c.type}"><span class="tag ${c.type}">${labels[c.type]}</span> <span class="meta">${statusLabels[c.status]}</span><h2>${esc(c.title)}</h2><p>${esc(c.summary)}</p><p class="scope"><strong>言える範囲：</strong>${esc(c.scope)}</p><p class="meta">対象：${esc(c.population)}<br>種別：${esc(c.design)}<br>タグ：${c.tags.map(esc).join(' · ')}</p><p class="links"><a href="${esc(c.siteUrl)}" target="_blank" rel="noreferrer">サイト記載</a>${c.sourceUrl?`<a href="${esc(c.sourceUrl)}" target="_blank" rel="noreferrer">原典</a>`:''}</p></article>`).join('')||'<p>該当項目はありません。</p>'}
fetch('data/claims.json').then(r=>r.json()).then(x=>{claims=x;render()}).catch(()=>{document.querySelector('#cards').innerHTML='<p>データを読み込めません。READMEのローカルサーバー手順で開いてください。</p>'});['search','type','status'].forEach(id=>document.querySelector('#'+id).addEventListener('input',render));
